.shop-card {
    /* Width is set on the TemplateContainer in SeedShopUI.cs — percentage on an inner
       element resolves against the auto-sized TemplateContainer, not the ScrollView. */
    padding: var(--spacing-md);
    align-items: center;
}

.shop-icon {
    width: 128px;
    height: 128px;
    border-radius: var(--radius-md);
    background-color: var(--color-bg-slot);
    margin-bottom: var(--spacing-sm);
}

.shop-seed-name {
    font-size: var(--font-lg);
    color: var(--color-text-bright);
    -unity-font-style: bold;
    margin-bottom: var(--spacing-xs);
    -unity-text-align: middle-center;
}

.shop-price {
    font-size: var(--font-md);
    color: var(--color-text-accent);
    margin-bottom: var(--spacing-xs);
}

.shop-condition {
    font-size: var(--font-sm);
    color: rgb(175, 200, 215);
    -unity-text-align: middle-center;
    margin-bottom: var(--spacing-sm);
    white-space: normal;
}

.shop-buy-btn {
    min-height: 96px;
    width: 100%;
    font-size: var(--font-md);
}

.shop-section-header {
    color: var(--color-text-dim);
    font-size: var(--font-sm);
    margin-top: var(--spacing-md);
    margin-left: var(--spacing-sm);
    margin-bottom: var(--spacing-xs);
    -unity-font-style: bold;
}
