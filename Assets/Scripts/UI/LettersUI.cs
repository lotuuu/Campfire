using UnityEngine;
using UnityEngine.UIElements;

namespace Garden
{
    public class LettersUI : MonoBehaviour
    {
        public void Initialize(VisualElement root)
        {
            // Stubbed for v1 -- placeholder text is already in the UXML
        }
    }
}
